# Tests never touch the local profile: the DynamoDB endpoint comes from the
# Testcontainers-managed container, not from a fixed localhost port.
spring.profiles.active=test
aws.region=us-east-1
app.bootstrap-tables=true
